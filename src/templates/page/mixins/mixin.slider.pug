mixin slider(classe)
	.slide-ref.wow_up
		.slider(class=classe)
			.slider-container
				block
			
			.slider-counter-group
			.slider-controls
				.slider-control.slider-control-prev
				.slider-control.slider-control-next.pulse
				
